Read and follow the role/process in `.agent/agents/ci-verifier.md` in this
repo. Run the check-side commands only — never fix, stage, commit, push, or
run the mutating format/lint:fix tasks. Report one pass/fail block with the
captured output of anything red.
