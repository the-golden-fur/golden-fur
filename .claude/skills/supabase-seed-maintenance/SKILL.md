---
name: supabase-seed-maintenance
description: The seed-coverage map (which tables are seeded, by which module, which are migration-seeded, which are deliberately app-only) plus the idempotent .ts + mirrored .sql + .spec.ts trio conventions and the change checklist. Use whenever a migration touches a seeded table or adds a reference/lookup table.
---

# Supabase seed maintenance

This is the Claude Code adapter for a tool-agnostic skill. Read and follow
the full instructions at
[.agent/skills/supabase-seed-maintenance.md](../../../.agent/skills/supabase-seed-maintenance.md)
before proceeding — that file is the canonical, maintained version (also
used by other AI coding tools working in this repo); this file exists only
so Claude Code can discover and auto-invoke it.
